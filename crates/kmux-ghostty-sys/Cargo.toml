[package]
name = "kmux-ghostty-sys"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Raw FFI bindings to libkmux_ghostty (Zig wrapper over libghostty-vt)."
links = "kmux_ghostty"
build = "build.rs"

[lib]
path = "src/lib.rs"

[dependencies]

[build-dependencies]
